# Substituido por ci-cd.yml — este arquivo pode ser removido apos confirmar que ci-cd.yml esta verde.
name: CI (legado)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow migrado para ci-cd.yml"
